# ===================================
#  Form Field Definitions
# ===================================

fields:
    author:
        label: keios.support::lang.fields.author
        type: dropdown
    content:
        label: keios.support::lang.fields.content
        type: richeditor
    is_support:
        label: keios.support::lang.fields.is_support
        type: switch
        default: true
        span: left